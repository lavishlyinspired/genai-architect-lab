uvicorn main:app --reload

docker version
docker ps
docker ps -a
docker help

# build the image from current project repo
docker build -t document-ananlysis-portal .
# run this image inside the container
docker run -d -p 8093:8080 --name doc-portal-container document-ananlysis-portal

# build the docker image.
    docker build -t myapp .
# run the image inside the docker container
# docker run -d -p 8080:8080 --name doc-portal Document-Analysis-Portal
docker run -d -p 8080:8080 --name doc-portal Document-Analysis-Portal