from setuptools import setup, find_packages

setup(
    name="Document Analysisi Portal",
    author="Akash",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        "structlog",
        # add other dependencies here
    ],
)
