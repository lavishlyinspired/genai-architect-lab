from setuptools import setup, find_packages

setup(
    name = "Information -retrieval-app",
    version = "0.0.1",
    author = 'akash',
    author_email='',
    packages = find_packages(),
    install_requires = []
)